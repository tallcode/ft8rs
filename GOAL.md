# ft8rs 项目目标与优化方向

## 项目定位

ft8rs 是一个精确的 FT8 解码器，目标是**100% 对齐 WSJT-X 的灵敏度**（不减损任何信号）。

**非实时解码器** — 对时间复杂度有较大容忍性（目标 < 180s），但绝不做任何精度/灵敏度妥协。

## 核心原则

1. **灵敏度第一** — 任何优化不得减少解码消息数（基线：≥20/20）
2. **精确对齐 WSJT-X** — 逐行对照 Fortran 源码，确保行为一致
3. **可读性优先于极致性能** — 保持代码清晰，让算法逻辑透明
4. **质量门控** — `test_20_message_baseline` 断言 ≥20 条，CI 阻断回退

## 当前基线

| 指标 | 值 | 状态 |
|------|------|------|
| 测试文件 | 210703_133430.wav | — |
| 解码数 | 20/20 | ✅ |
| 耗时 (release) | ~28s | ✅ < 180s |
| 测试覆盖 | 9 个测试 | ✅ |

## 已完成的关键突破

### subtractft8 精确信号减法 (2026-05-17)
- **问题**: 原有减法不精确，4 条弱信号漏解
- **根因**: 3 个 bug — FFT 尺寸不匹配、IFFT 归一化差异、cshift 方向错误
- **方案**: 时域循环卷积（cos² 窗口，4001 点），完全避免 FFT 精度问题
- **结果**: 16→20 条 (+25%)

## 优化方向（保持精度前提下加速）

### P0: subtract_ft8 FFT 加速
- **当前**: 时域卷积 O(N×M) = 151680 × 4001 ≈ 607M 操作/次减法
- **目标**: FFT 卷积 O(N·logN) ≈ 14M 操作/次减法（43× 加速）
- **方案**: 
  1. NFFT = 262144 (pow2)
  2. 零填充 camp 和 window 到 NFFT
  3. FFT(camp) × FFT(window) → IFFT → 线性卷积
  4. Window 无需 cshift（线性卷积不需要循环偏移）
  5. 无需 endcorrection（线性卷积边界正确）
- **预期**: 34s → ~15s

### P1: BP 解码器优化
- **当前**: max_iterations=100, 每次 O(N)
- **分析**: WSJT-X 最多用 36 次迭代，我们 100 次是保守值
- **方案**: 动态自适应迭代（早停 + 最大 50次）

### P2: sync8 频谱计算优化
- **当前**: 4096 点 FFT，每帧 372 个符号窗口
- **分析**: 与 WSJT-X 一致，无优化空间（已在最优）

### P3: 并行化
- **方案**: 对独立候选信号并行解码
- **限制**: 需要处理共享残差的正确同步

## 不做的事

- ❌ 任何降低精度的"快速"近似
- ❌ 降低 BP 迭代次数（除非证明不影响收敛）
- ❌ 简化 GFSK 波形为方波等近似
- ❌ 降低 FFT 分辨率
- ❌ 跳过任何 WSJT-X 已有的解码步骤

## 参考项目

- **WSJT-X v2.7.0** (`wsjtx/lib/ft8/`) — 权威参考实现
- **ft8ts** (`ft8ts/src/index.ts`) — TypeScript 参考端口（16/20）
- **wsjtx_lib** (`wsjtx_lib/`) — C++ 封装 Fortran（用于交叉验证）
